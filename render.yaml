services:
  - type: web
    name: wetransfer-app
    env: python3
    region: oregon
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 app:app
    envVars:
      - key: EMAIL_SENDER
        value: nwekee125@gmail.com
      - key: EMAIL_PASSWORD
        value: mhhihywubresapns
      - key: EMAIL_RECEIVER
        value: maxwell202201@gmail.com
      - key: PORT
        value: "5000"
